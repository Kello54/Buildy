site_name: V18 Platform Docs

theme:
  name: material

plugins:
  - search
  - swagger-ui:
      spec: docs/platform.json   # ✅ fixed 'spec_file' -> 'spec'

nav:
  - Home: index.md
  - Runbooks: runbooks.md
  - Dashboards: dashboards.md
  - API: api.md                  # ✅ added so API page shows in sidebar
